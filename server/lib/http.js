export function json(response, status, body) { response.writeHead(status, { 'Content-Type': 'application/json; charset=utf-8', 'Access-Control-Allow-Origin': '*', 'Access-Control-Allow-Headers': 'Content-Type, Authorization', 'Access-Control-Allow-Methods': 'GET, POST, PUT, OPTIONS' }); response.end(JSON.stringify(body)) }
export async function readJson(request) { let body = ''; for await (const chunk of request) body += chunk; try { return body ? JSON.parse(body) : {} } catch { throw Object.assign(new Error('Body JSON tidak valid.'), { status: 400 }) } }
export function route(url, pattern) { const match = url.pathname.match(pattern); return match?.slice(1).map(decodeURIComponent) }
